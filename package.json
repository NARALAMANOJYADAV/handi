{
  "name": "handivoice",
  "version": "1.0.0",
  "scripts": {
    "install": "cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm run build && rm -rf public && mv client/dist public"
  }
}
