{
  "name": "handivoice",
  "version": "1.0.0",
  "scripts": {
    "install": "cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "npm install concurrently --no-save && npx concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  }
}
